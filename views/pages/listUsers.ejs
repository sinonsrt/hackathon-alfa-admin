<!DOCTYPE html>
<html lang="pt-br">

<head>
    <%- include ../layouts/header %>
</head>

<body>
    <%- include ../layouts/navbar %>

    <div class="container mt-5">

        <div class="row my-3">
            <div class="col">
                <h2 class="text-start">Usuários</h2>
            </div>
            <div class="col-auto">
                <a href="cadastroUsers" class="btn btn-success float-end">Adicionar</a>
            </div>
        </div>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nome</th>
                    <th scope="col">Login</th>
                    <th scope="col">Ações</th>
                </tr>
            </thead>
            <tbody>
                <% for(let d of data) {%>
                <tr class="align-middle">
                    <th scope="row"><%= d.id %></th>
                    <td><%= d.nome %></td>
                    <td><%= d.login %></td>
                    <td>
                        <a href="editUsers/<%=d.id%>" class="btn btn-success"><i class="fas fa-pencil-alt"></i></a>
                        <a href="excluirUsers/<%=d.id%>"" class="btn btn-danger"><i class="fas fa-trash-alt"></i></a>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <%- include ../layouts/footer %>
</body>
</html>